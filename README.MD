Endpoint URLs
->  http://localhost:3000
    -> POST Endpoints
        -> .../api/greet
            -> Example JSON Response
                -> {"timeOfDay": "Evening", "language": "English", "tone": "Casual"}
                    -> {"greetingMessage":"Night!"}
    -> GET Endpoints
        -> .../api/times-of-day
        -> .../api/languages